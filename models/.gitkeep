# models/.gitkeep
# This folder stores:
#   emotion_cnn.h5                  — trained CNN weights (generated by scripts/train_emotion_cnn.py)
#   haarcascade_frontalface_default.xml  — downloaded via OpenCV or cv2.data
# Neither file is committed to git (see .gitignore).
