# data/.gitkeep
# This folder stores:
#   task_optimizer.db   — SQLite database (auto-created on first run)
#   tasks.csv           — task catalogue (generated by scripts/seed_tasks.py)
#   fer2013/            — FER-2013 dataset (download separately, not committed)
# Neither file is committed to git (see .gitignore).
